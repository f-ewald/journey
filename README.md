# Map Journey

Frontend-only Vite app built on [`@f-ewald/components`](https://www.npmjs.com/package/@f-ewald/components).

## Development

```bash
npm install
npm run dev      # dev server
npm run build    # typecheck + production build into dist/
npm run preview  # serve the production build
```
